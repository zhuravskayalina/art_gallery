import React from 'react';
import SearchBar from '../SearchBar/SearchBar';

class Home extends React.Component {
  render() {
    return (
      <main>
        <section>
          <SearchBar />
        </section>
      </main>
    );
  }
}

export default Home;
